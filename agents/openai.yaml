interface:
  display_name: "中国求职 Skill"
  short_description: "中文简历、岗位搜索、定制投递工作流"
  default_prompt: "使用 $job-search-cn 规划中国求职流程：搜索岗位、分析 JD、定制简历、生成投递包；任何真实发送、上传、提交前都必须逐项确认。"
policy:
  allow_implicit_invocation: true
